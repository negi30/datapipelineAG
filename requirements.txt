# Pinned to modern, mutually-compatible versions (tested together).
# LangChain 1.x is a full API rewrite from the 0.x line -- older/mismatched
# installs are the most common cause of import errors in this project, so
# don't loosen these without retesting the agents/ modules.
fastapi>=0.139
uvicorn[standard]>=0.51
pandas>=2.2
python-dotenv>=1.0
python-multipart>=0.0.20
langchain>=1.3
langchain-openai>=1.3
langchain-community>=0.4
langchain-core>=1.4
rich>=13.0
plotly>=6.0
streamlit>=1.50
requests>=2.32
